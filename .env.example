# 复制为 .env.local 后填入；勿提交真实密钥（见 AGENTS.md）
OPENAI_API_KEY=

# 可选；默认 gpt-5.4（API 别名；可改为快照如 gpt-5.4-2026-03-05）
ANYU_OPENAI_CHAT_MODEL=gpt-5.4

# 可选；覆盖默认安语 system prompt 时用整段文本
# ANYU_SYSTEM_PROMPT=

# 可选；与 prompt 变更同步 bump
ANYU_PROMPT_VERSION=v0

# 持久化 session / 历史时再配
# DATABASE_URL=
